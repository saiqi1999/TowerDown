import { type SquadSpawnData } from './SquadTypes';

// Primitive era starts with empty roster slots. The first sword barracks creates the first squad.
export const STATIC_SQUADS: SquadSpawnData[] = [];
